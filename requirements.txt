# requirements.txt — convenience mirror of tls2dseg/pyproject.toml [project.dependencies]
# pyproject.toml is the source of truth. Keep this file in sync when bumping versions.
#
# Minimum pip >= 21.3 required for the `pkg @ git+https://...` PEP 508 URL form.
#
# Note: pyvips requires the system libvips library.
#   Debian/Ubuntu: apt install libvips-dev
#   Fedora/RHEL:   dnf install vips-devel

# ---- git+https workspace deps ----
sam-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f
pchandler @ git+https://github.com/gseg-ethz/PCHandler.git@develop/tomislav
pc2img @ git+https://github.com/gseg-ethz/pc2img.git@develop/tomislav

# ---- ML / vision foundation models ----
torch~=2.7
transformers~=5.1
supervision~=0.25

# ---- Image processing ----
opencv-python~=4.11
Pillow~=10.4
imageio~=2.37
pyvips~=3.0
matplotlib~=3.10
pycocotools~=2.0

# ---- Scientific / clustering ----
scipy~=1.15
scikit-learn~=1.6
hdbscan~=0.8
joblib~=1.4
igraph~=0.11
leidenalg~=0.10
trimesh~=4.6
networkx~=3.4
numpy~=1.26

# ---- Config + CLI ----
pydantic~=2.8
pydantic-settings[yaml]~=2.14
typer>=0.13
