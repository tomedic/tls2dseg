"""Phase 1 smoke test — verifies pytest is wired.

Replaced by real unit tests in Phase 4 (TEST-* requirements).
"""


def test_pytest_wired() -> None:
    """Trivially passes — confirms the test runner discovers and runs at least one test."""
    assert True
